[graph]
# Dev-only dependencies never ship in the release binary, so their license
# doesn't affect distribution.
exclude-dev = true

[licenses]
allow = [
    "MIT",
    "Apache-2.0",
    "BSD-2-Clause",
    "BSD-3-Clause",
    "Unicode-3.0",
    "Zlib"
]
